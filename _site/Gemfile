source "https://rubygems.org"

# Remove specific jekyll version and use github-pages to match the server environment
gem "github-pages", group: :jekyll_plugins

# Webrick is required for Ruby 3.0+ local development
gem "webrick"
